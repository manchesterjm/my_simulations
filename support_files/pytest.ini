[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    fuzz: marks fuzz tests using hypothesis

[hypothesis]
max_examples = 100
deadline = 5000
